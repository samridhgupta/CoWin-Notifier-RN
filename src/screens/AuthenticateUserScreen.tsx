import React, { FunctionComponent } from 'react';
import { View } from 'react-native';

const AuthenticateUserScreen: FunctionComponent = () => {
    return <View></View>;
};

export default AuthenticateUserScreen;
